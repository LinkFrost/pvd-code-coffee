import Link from "next/link";

export default function HomePage() {
  return (
    <main className="mb-0 mt-0 flex min-h-screen flex-1 flex-col items-center justify-center align-middle">
      <span className="text-3xl font-bold">Under Construction...</span>
    </main>
  );
}
